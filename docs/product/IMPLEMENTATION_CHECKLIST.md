# Implementation Checklist

This checklist tracks completed work and next steps based on the project documentation. Update it whenever a task is finished so the repository status stays aligned with the docs.

---

## Checklist Rules

*   Use GitHub Markdown checkboxes: `[ ]` for pending, `[x]` for completed.
*   Link each checklist item to the source document that defines the requirement.
*   Move finished work into the relevant phase section instead of creating disconnected notes.
*   Keep claims conservative: only mark implementation items complete when code, docs, and basic verification are done.
*   Add a short verification note when a completed item was tested or reviewed.

---

## Documentation Alignment

- [x] Confirm Python as the desktop application stack. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md), [ARCHITECTURE.md](ARCHITECTURE.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: docs updated to specify PySide6/Qt and PyInstaller.
- [x] Add a documentation-based Markdown checklist for future work tracking. Source: [README.md](README.md), [ROADMAP.md](ROADMAP.md), [docs README](README.md). Verification: this checklist file was added and indexed.

---

## Phase 1: Core Engine & CLI [MVP]

- [x] Build pipeline to stream video frames to/from FFmpeg subprocesses. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: `python -m unittest` covers FFmpeg command construction and pipeline integration boundaries.
- [x] Connect ONNX Runtime with execution providers. Source: [ARCHITECTURE.md](ARCHITECTURE.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: provider selection is implemented and covered by unit tests for auto, CPU, and accelerator fallback ordering.
- [x] Auto-tuning Hardware Benchmarker startup warmup profiling script. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: `run_onnx_provider_benchmark` performs real upscaler warmup calls per provider when an ONNX model/runtime is available, and `--benchmark` is wired into the pipeline.
- [x] Model registry SHA256 integrity verification. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: model cache resolution and SHA256 verification are implemented in `models/registry.py` and covered by unit tests.
- [x] Decoupled Streaming Audio-Video Muxer pipeline. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: FFmpeg writer accepts raw video on stdin plus optional audio/metadata inputs in one process, avoiding the previous temporary video then mux step.
- [x] Perceptual Audio Restoration Pipeline using FFmpeg FFT denoiser integration. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: `--audio-restore` runs an FFmpeg `afftdn` audio restoration command before final muxing.
- [x] Automated Bitrate Calibration from short-duration spatial complexity scans. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: pipeline runs an FFmpeg `signalstats` short scan and applies the resulting complexity score to maxrate/bufsize calibration.
- [x] Implement a single-model upscaler. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: pipeline selects a cached ONNX model when available and falls back to FFmpeg Lanczos baseline when model/runtime files are absent.
- [x] Non-blocking CLI Progress Monitor Dashboard. Source: [ARCHITECTURE.md](ARCHITECTURE.md), [ROADMAP.md](ROADMAP.md). Verification: queue-backed `CliProgressMonitor` is covered by unit tests and used by CLI enhancement runs.
- [x] Automatic Frame Padding & Cropping with reflective margins for ONNX compatibility. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: RGB frame bytes are reflect-padded before ONNX inference and cropped back to target output dimensions after inference.
- [x] Metadata Preservation pipeline for subtitles, chapters, and camera tags. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: final mux command maps optional subtitles, chapters, and source metadata unless `--no-metadata` is set.
- [x] Create standard CLI controls. Source: [CLI_WORKFLOW.md](CLI_WORKFLOW.md). Verification: `python -m unittest` covers documented CLI flags and dry-run parsing.
- [x] Dynamic Resource Governor with `--quiet` throttling to reduce overheating and OS lag. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: `--quiet` enables a resource governor that throttles the frame loop and is covered by unit tests.

---

## Phase 2: Temporal Consistency & Advanced Filtering [Planned]

- [x] Add temporal consistency checks and scene cut detection. Source: [ROADMAP.md](ROADMAP.md). Verification: `TemporalAnalyzer` is integrated into the main frame loop and evaluates each frame before enhancement.
- [x] Intelligent Frame Skip based on frame SSIM delta. Source: [ROADMAP.md](ROADMAP.md). Verification: near-duplicate frames reuse the previous processed output in the main frame loop.
- [x] Custom ONNX Model Hot-Swapping with auto-scan for drop-in community models. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: model discovery scans the model cache for `.onnx` files and merges community models into a runtime registry.
- [x] Face Restoration Pipeline using GFPGAN/CodeFormer ONNX integration. Source: [ROADMAP.md](ROADMAP.md). Verification: FaceRestorer performs face detection and crop/merge routing dynamically using helper ROI mapping.
- [x] Dynamic Spatial-Temporal Super-Resolution scaling. Source: [ROADMAP.md](ROADMAP.md). Verification: runner dynamically switches active upscaler scale factor on a frame-by-frame basis, adjusting resolutions adaptively.
- [x] Multi-destination Video Encoding with selective time range processing and stream copy. Source: [ROADMAP.md](ROADMAP.md). Verification: runner emits configured extra destinations after the primary output, using file copy for full outputs and FFmpeg for time ranges.
- [x] Async GPU Double Buffering and Thread-Pool Post-Processing. Source: [ROADMAP.md](ROADMAP.md). Verification: DoubleBuffer is integrated into the runner main frame loop to asynchronously stage frames during processing.
- [x] Automated VRAM/RAM Limit Detection and Auto-Tiling Factor config. Source: [ROADMAP.md](ROADMAP.md), [TROUBLESHOOTING.md](TROUBLESHOOTING.md). Verification: resource helpers detect RAM and choose tile sizes based on available RAM/VRAM budget.
- [x] Pause and Resume Checkpoint System with ZSTD/LZ4 Lossless Caching. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: checkpoint state is loaded by the runner, cached frames are reused on resume, and processed frames/state are saved during processing.
- [x] Multi-Model Chaining interface for sequential pipelines. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: runner builds a `ModelChain` from restoration operations, face restoration, and the selected upscaler.
- [x] Region of Interest selective processing options. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: ROI-only extraction, processing, and paste-back are wired into the runner for scale-1 selective enhancement.
- [x] Multi-GPU and Distributed Local Pipeline support. Source: [ROADMAP.md](ROADMAP.md). Verification: DistributedFrameProcessor spawns and assigns frames round-robin across configured local devices.
- [x] Add denoise, deblur, and compression artifact cleanup models. Source: [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md), [ROADMAP.md](ROADMAP.md). Verification: enabled restoration ops are inserted into the runner's model chain before upscaling.
- [x] Enable batch command operations for folder input files. Source: [ROADMAP.md](ROADMAP.md), [CLI_WORKFLOW.md](CLI_WORKFLOW.md). Verification: `--batch` discovers supported video files and builds per-file enhancement configs.
- [x] Add FP16 quantization support. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: ONNX frame processors switch input tensors to float16 when `--fp16` is requested and the selected provider supports it.

---

## Phase 3: Python Desktop UI & Packaging [Future]

- [x] Interactive HTML Comparison Report Export. Source: [ROADMAP.md](ROADMAP.md), [QUALITY_METRICS.md](QUALITY_METRICS.md). Verification: report renderer writes self-contained HTML with side-by-side videos and metric rows.
- [x] Visual Comparator GUI Tool for preview single-frame splits. Source: [ROADMAP.md](ROADMAP.md), [UI_WORKFLOW.md](UI_WORKFLOW.md). Verification: comparator state model tracks original/enhanced paths and normalized split position.
- [x] Local WebUI Host Dashboard. Source: [ROADMAP.md](ROADMAP.md). Verification: local WebUI config exposes dashboard URL/status scaffold.
- [x] Local LAN Render Farm distributed processing node network. Source: [ROADMAP.md](ROADMAP.md). Verification: render farm planner shards frame ranges across LAN render nodes.
- [x] Offline Model Encryption for model copy-protection. Source: [ROADMAP.md](ROADMAP.md). Verification: model encryption helpers encrypt/decrypt model bytes and support file encryption.
- [x] Smart Power Governor and low-battery management. Source: [ROADMAP.md](ROADMAP.md). Verification: power governor pauses when battery is below threshold and unplugged.
- [x] Intelligent Hardware Thermal Throttling Monitor. Source: [ROADMAP.md](ROADMAP.md). Verification: thermal monitor computes throttling delay when temperature exceeds threshold.
- [x] Automated Subtitle OCR and Translation. Source: [ROADMAP.md](ROADMAP.md). Verification: subtitle job planner records OCR source language and optional translation target.
- [x] Visual Artifact Anomaly Detector. Source: [ROADMAP.md](ROADMAP.md). Verification: artifact detector flags black/corrupted frames from RGB byte statistics.
- [x] Desktop Notification System. Source: [ROADMAP.md](ROADMAP.md). Verification: notification command planner supports macOS, Linux, and Windows command shapes.
- [x] Intelligent Scene-based Model Selection. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: scene classifier maps brightness/motion categories to model choices.
- [x] Local Frame Interpolation using RIFE planning. Source: [ROADMAP.md](ROADMAP.md). Verification: interpolation planner computes RIFE target FPS factor.
- [x] Build a user-friendly Python desktop GUI with PySide6/Qt. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md), [ROADMAP.md](ROADMAP.md). Verification: desktop module compiles and exposes a PySide6 main window.
- [x] Develop a media player showing side-by-side visual comparisons. Source: [README.md](../README.md), [UI_WORKFLOW.md](UI_WORKFLOW.md). Verification: desktop UI includes original/enhanced preview panels and a separate comparator state model.
- [x] Setup PyInstaller workflows to output offline installers. Source: [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md), [ROADMAP.md](ROADMAP.md). Verification: PyInstaller command helper builds a one-file offline package command.
- [x] Wire the desktop UI worker to the real enhancement pipeline instead of the placeholder job. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md), [ARCHITECTURE.md](ARCHITECTURE.md). Verification: desktop worker calls `run_enhancement` with the same shared `EnhancementConfig` used by the CLI while emitting Qt progress from a background thread.
- [x] Add drag-and-drop video input support to the desktop media selection panel. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md). Verification: `VideoPathEdit` accepts local supported video drops and derives a default enhanced output path.
- [x] Implement sampled-frame preview rendering for the desktop comparator. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md), [README.md](../README.md). Verification: comparator helper extracts original/enhanced preview crops with FFmpeg without processing the full video.
- [x] Add an interactive split-slider interface to the HTML comparison report. Source: [QUALITY_METRICS.md](QUALITY_METRICS.md), [ROADMAP.md](ROADMAP.md). Verification: report HTML includes self-contained CSS/JavaScript and a range slider that clips the enhanced video over the original.
- [x] Compute and include PSNR, SSIM, VMAF, FPS, and hardware provider metadata in comparison reports. Source: [QUALITY_METRICS.md](QUALITY_METRICS.md). Verification: report helpers collect FFmpeg PSNR/SSIM/VMAF when available and render FPS/provider metadata in the static HTML output.
- [x] Build the Local WebUI dashboard as a real local host instead of a status scaffold. Source: [ROADMAP.md](ROADMAP.md). Verification: stdlib HTTP dashboard serves HTML and `/api/status` JSON backed by thread-safe dashboard state.
- [x] Implement executable LAN render farm coordination beyond frame sharding plans. Source: [ROADMAP.md](ROADMAP.md). Verification: `RenderFarmCoordinator` dispatches shard payloads to LAN node `/render` endpoints and records success/failure results.
- [x] Implement subtitle OCR and optional translation execution beyond job planning. Source: [ROADMAP.md](ROADMAP.md). Verification: subtitle workflow extracts sampled frames with FFmpeg, calls an OCR engine, applies optional translation, and writes SRT cues.
- [x] Run RIFE frame interpolation through the processing pipeline beyond target-FPS planning. Source: [ROADMAP.md](ROADMAP.md). Verification: interpolation runner invokes an injected RIFE runner when available and falls back to FFmpeg interpolation for target-FPS output.
- [x] Expand packaging from a PyInstaller command helper to platform installer workflows for `.exe`, `.dmg`, and `.deb`. Source: [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md), [ROADMAP.md](ROADMAP.md). Verification: packaging helpers build CLI/desktop PyInstaller commands with optional FFmpeg, PySide6/Qt, and ONNX Runtime bundles plus native installer command plans.

---

## Phase 4: Production Release & Advanced Optimization [Completed]

- [x] Build a release-grade installer pipeline for Windows, macOS, and Linux. Source: [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: release helpers plan CLI/desktop PyInstaller builds and native installer commands for `.exe`, `.dmg`, and `.deb`, with optional FFmpeg, PySide6/Qt, and ONNX Runtime bundle paths.
- [x] Add first-run model setup and offline model import workflows. Source: [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: model setup helpers inspect missing/imported models, copy local `.onnx` files into the model cache, verify SHA256, and write import metadata for offline registration.
- [x] Add production QA automation for packaged builds. Source: [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md), [TROUBLESHOOTING.md](TROUBLESHOOTING.md). Verification: packaged CLI smoke helpers launch a built executable with `--help` and can run a tiny `--dry-run` fixture check.
- [x] Add desktop job cancellation, pause/resume controls, and notification integration. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md), [ROADMAP.md](ROADMAP.md). Verification: shared job queue supports cancel, pause, resume, complete, and fail transitions, and completion/failure paths send desktop notifications.
- [x] Add WebUI job submission and queue management. Source: [ROADMAP.md](ROADMAP.md), [CLI_WORKFLOW.md](CLI_WORKFLOW.md). Verification: `JobQueue` tracks submitted, running, cancelled, and completed enhancement jobs, and the local dashboard exposes `/api/jobs` list/create/cancel endpoints.
- [x] Define and implement a stable LAN render node protocol. Source: [ROADMAP.md](ROADMAP.md). Verification: render node server/state exposes health, capability, render, cancel, and result operations over a small HTTP contract.
- [x] Add render farm result merge and failure recovery. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: render farm coordinator retries failed shard dispatches and FFmpeg concat helper merges completed shard outputs losslessly.
- [x] Add a benchmark dataset runner for PSNR, SSIM, VMAF, and visual artifact scoring. Source: [QUALITY_METRICS.md](QUALITY_METRICS.md). Verification: dataset benchmark helpers discover original/enhanced pairs, collect quality metrics, run artifact checks, write JSON summaries, and flag baseline regressions.
- [x] Wire RIFE/AI frame interpolation into the main enhancement runner as an optional output stage. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: `--target-fps` maps into shared config and runner applies interpolation after enhancement when the requested FPS is above source FPS.
- [x] Integrate real OCR and translation engines for subtitle workflows. Source: [ROADMAP.md](ROADMAP.md), [IDEAS_AND_RESEARCH.md](IDEAS_AND_RESEARCH.md). Verification: subtitle pipeline supports local command-based OCR and translation adapters that can be plugged into the sampled-frame OCR workflow.
- [x] INT8 Model Quantization support for CPU/NPU. Source: [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: quantization planner selects INT8 for capable CPU/NPU providers and ONNX Runtime quantization helper writes INT8 model outputs when quantization tooling is installed.
- [x] Distributed LAN Transcoding and adaptive frame merging. Source: [ROADMAP.md](ROADMAP.md). Verification: distributed transcoding planner shards video duration across LAN nodes, assigns segment outputs round-robin, and selects stream-copy merge mode for multi-segment outputs.
- [x] Local-first Encrypted Sync for user configuration presets. Source: [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md). Verification: preset helpers export encrypted enhancement configs, import them back into `EnhancementConfig`, and copy encrypted preset files into local sync folders.
- [x] Visual Video Timeline Crop Preview. Source: [UI_WORKFLOW.md](UI_WORKFLOW.md). Verification: comparator helpers plan evenly spaced timeline crop previews and render multi-frame original/enhanced preview pairs using the sampled-preview extractor.

---

## Phase 5: Advanced Media & Runtime Expansion [Completed]

- [x] Vulkan API & WebGPU execution provider support. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: runtime provider selection accepts Vulkan/WebGPU provider names and experimental provider planner builds CPU-fallback adapter plans.
- [x] OpenVINO and QNN execution provider support for Intel and Qualcomm NPU acceleration. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: runtime provider selection and accelerator planner detect OpenVINO/QNN provider availability and fall back safely to CPU when unsupported.
- [x] 3D LUT lookup tables (.cube) support. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: color helpers parse local `.cube` LUT files, validate LUT size/value counts, and emit FFmpeg `lut3d` filter arguments.
- [x] HDR, 10-bit, and wide-gamut color pipeline support. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: HDR planner emits BT.2020/PQ/HLG color metadata arguments and 10-bit pixel formats for FFmpeg output planning.
- [x] SDR/HDR tone-mapping presets for controlled color-space conversion. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: tone-map planner emits validated FFmpeg filter chains for SDR-to-HDR, HDR-to-SDR, and HDR passthrough workflows.
- [x] Professional codec and delivery presets for AV1, ProRes, DNxHR, HEVC 10-bit, and archival mezzanine exports. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md), [CLI_WORKFLOW.md](CLI_WORKFLOW.md). Verification: delivery preset helpers generate validated FFmpeg argument sets for AV1, HEVC 10-bit, ProRes, DNxHR, and archival FFV1 exports.
- [x] Hardware encoder auto-profiling for NVENC, QSV, AMF, and VideoToolbox. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: encoder profiler parses FFmpeg encoder availability and selects a platform-safe hardware encoder profile with quality hints.
- [x] High-resolution tiled render planning for 8K/16K output workflows. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: tiled render planner computes memory-safe tile size, overlap margins, tile grid, and estimated per-tile memory for very high-resolution outputs.
- [x] Model distillation and pruning script helper tools. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: model optimization helpers plan pruning/distillation commands, run local optimization tools, verify optimized ONNX output creation, and return output SHA256 metadata.
- [x] Advanced model metadata validation for custom ONNX compatibility, tensor shapes, opsets, and scale factors. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: custom ONNX validator checks sidecar metadata for tensor shape, channel count, opset, and scale compatibility with actionable diagnostics.
- [x] Versioned local model package format for optimized ONNX bundles and sidecar metadata. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: model package helpers create versioned bundle manifests, copy model/metadata files, verify SHA256 integrity, enforce minimum versions, and import bundles into the model cache.

## Phase 6: Headless API & Operations [Completed]

- [x] Standalone REST API server mode. Source: [ROADMAP.md](ROADMAP.md). Verification: local REST server exposes job list, submit, inspect, cancel, health, readiness, diagnostics, and OpenAPI endpoints using the shared job queue.
- [x] OpenAPI schema export and local client tooling. Source: [ROADMAP.md](ROADMAP.md), [CLI_WORKFLOW.md](CLI_WORKFLOW.md). Verification: OpenAPI helper documents job and health endpoints, and local REST client can submit, inspect, and cancel jobs through the documented contract.
- [x] REST API authentication, rate limits, and local network access controls. Source: [ROADMAP.md](ROADMAP.md). Verification: API config supports bearer tokens, allowed client host checks, and per-client request rate limiting.
- [x] Durable job queue storage for headless restart recovery. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md). Verification: queue storage writes job state to JSON and reloads running jobs as queued recovery candidates after restart.
- [x] Headless worker pool with configurable concurrency, device affinity, and queue priorities. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md). Verification: worker pool config limits concurrent assignments and maps queued jobs to priority-ordered device-affinity worker profiles.
- [x] Retry, timeout, and backoff policies for failed API and render-node jobs. Source: [ROADMAP.md](ROADMAP.md), [TROUBLESHOOTING.md](TROUBLESHOOTING.md). Verification: retry policy helper applies exponential backoff, max attempts, and timeout accounting around failed operations.
- [x] Graceful shutdown and drain mode for long-running render services. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md). Verification: service control enters drain mode, rejects new job acceptance, and persists current queue state before shutdown.
- [x] LAN node discovery and capability advertisement for local render services. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md). Verification: discovery helpers encode/decode render node capability advertisements and filter compatible nodes by required provider.
- [x] Health, readiness, and diagnostics endpoints for local service supervision. Source: [ROADMAP.md](ROADMAP.md), [TROUBLESHOOTING.md](TROUBLESHOOTING.md). Verification: API diagnostics report service health, queued jobs, FFmpeg availability, model cache readiness, missing models, and total job count.
- [x] Live job log streaming and progress event replay for disconnected clients. Source: [ROADMAP.md](ROADMAP.md), [CLI_WORKFLOW.md](CLI_WORKFLOW.md). Verification: event log stores ordered job events, replays events after a client-provided ID, and renders Server-Sent Events payloads.
- [x] Disk quota and temporary workspace cleanup controls for headless deployments. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: workspace cleanup planner selects old inactive files until quota targets are met and skips active render artifacts.
- [x] Service profile configuration for localhost-only, LAN-shared, and render-node modes. Source: [ROADMAP.md](ROADMAP.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: service profile presets configure bind address, token requirement, worker limits, discovery behavior, and container readiness.
- [x] Local observability dashboard for render telemetry, hardware history, job logs, and quality trend tracking. Source: [ROADMAP.md](ROADMAP.md), [QUALITY_METRICS.md](QUALITY_METRICS.md), [UI_WORKFLOW.md](UI_WORKFLOW.md). Verification: telemetry store records per-job FPS/provider/temperature/memory/error/quality values and produces dashboard payload summaries.
- [x] Containerized headless deployment profile for Docker/Podman render nodes. Source: [ROADMAP.md](ROADMAP.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: container profile helper renders an FFmpeg-ready Containerfile exposing the headless REST/render service port.

## Phase 7: Ecosystem Governance & Lifecycle [Completed]

- [x] Plugin/extension SDK for custom model stages, FFmpeg filters, and export hooks. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: sandboxed plugin runtime pipes model-stage plugins through the frame processor, collects FFmpeg filter plugins for writer commands, and runs export hooks after completed renders.
- [x] Plugin sandboxing and permission model for safe local extensions. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](ARCHITECTURE.md). Verification: core plugin runtime routes stage hooks through `SandboxedRegistry`, enforcing declared permissions for model and FFmpeg filter plugin calls.
- [x] Workflow automation profiles for reusable enhancement recipes and scheduled local jobs. Source: [ROADMAP.md](ROADMAP.md), [CLI_WORKFLOW.md](CLI_WORKFLOW.md), [UI_WORKFLOW.md](UI_WORKFLOW.md). Verification: `WorkflowScheduler` evaluates cron schedules, prevents duplicate same-minute enqueues, converts profile params into `EnhancementConfig`, and submits due profiles to the shared job queue.
- [x] Export artifact manifest generation for reproducible outputs. Source: [ROADMAP.md](ROADMAP.md), [QUALITY_METRICS.md](QUALITY_METRICS.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: completed local renders now write a sidecar manifest containing settings, source/output fingerprints, metrics hooks, and manifest digest support.
- [x] Configuration backup, restore, and migration tooling. Source: [ROADMAP.md](ROADMAP.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md). Verification: config directory backup/restore helpers export canonical presets/plugins/server JSON files, restore them to a target config directory, and preserve migration-chain compatibility.
- [x] Local audit log for API requests, plugin actions, job lifecycle events, and permission changes. Source: [ROADMAP.md](ROADMAP.md). Verification: REST API requests, job queue lifecycle events, plugin actions, and sandbox permission denials are recorded through `AuditLog` with sensitive path/token fields stripped.
- [x] Cross-platform preset compatibility matrix tests. Source: [ROADMAP.md](ROADMAP.md), [BUILD_AND_PACKAGING.md](BUILD_AND_PACKAGING.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: default matrix validation covers platform/provider combinations, current-environment validation, and JSON compatibility report generation.
- [x] Offline update verification and model rollback manager. Source: [ROADMAP.md](ROADMAP.md). Verification: offline update manifests support SHA-256 integrity plus HMAC-SHA256 signatures, signed verification failure paths, rollback snapshots, restore, and pruning.
- [x] Local model signature verification and quarantine manager. Source: [ROADMAP.md](ROADMAP.md), [MODELS_AND_INFERENCE.md](MODELS_AND_INFERENCE.md). Verification: strict ONNX safe-op verification rejects unknown/custom operators and `verify_or_quarantine_model` immediately isolates failed imports with persisted quarantine records.
- [x] Local hardware performance and runtime telemetry collector. Source: [ROADMAP.md](ROADMAP.md), [TROUBLESHOOTING.md](TROUBLESHOOTING.md). Verification: enhancement runs and REST job submissions can record sanitized telemetry entries, and encrypted diagnostic bundle export remains covered.
- [x] Database defragmentation and cache compaction utility. Source: [ROADMAP.md](ROADMAP.md), [VIDEO_PIPELINE.md](VIDEO_PIPELINE.md). Verification: `run_local_maintenance` combines SQLite job pruning/VACUUM and cache age/size compaction into one maintenance result.
- [x] Offline SDK documentation and developer guide generator. Source: [ROADMAP.md](ROADMAP.md). Verification: `build_offline_sdk_docs` builds the default self-contained SDK reference from the repository pipeline modules.

---

## Phase 8: Desktop UX & Batch Processing [Completed]

- [x] Multi-file drag-and-drop support in the desktop UI. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: `VideoPathEdit` accepts all supported local video files from a drop event and adds them to `DesktopQueueModel` without duplicating existing queue entries.
- [x] File queue table in the desktop UI with per-file status columns (Pending, Processing, Done, Error). Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: desktop UI owns a `QTableWidget` backed by `DesktopQueueModel`, rendering input, output, status, progress, and message columns per file.
- [x] Batch worker that processes a list of `EnhancementConfig` entries sequentially with per-file progress emission and cancellation support. Source: [ROADMAP.md](ROADMAP.md), [ARCHITECTURE.md](../ARCHITECTURE.md). Verification: `BatchEnhancementWorker` runs `run_batch_jobs`, emits per-file table updates, and uses `BatchCancelToken` to stop before the next queued file.
- [x] Auto output path derivation for batch jobs: `[stem]_enhanced.mp4` written to the same directory as each input file, with no user prompt required. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: `DesktopQueueModel` derives per-file output paths with `default_batch_output_path` and maps them into batch `EnhancementConfig` entries automatically.
- [x] Add/Remove file controls in the desktop queue: users can remove individual files from the queue before starting, and can add more files via Browse or drag-and-drop while the queue is idle. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: desktop UI exposes Add Files and Remove Selected controls, while `DesktopQueueModel.remove_many` removes selected queue rows without disturbing remaining order.
- [x] Per-file progress bar in the queue table that updates in real time during processing. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: queue table renders each row's progress with a `QProgressBar` cell widget and refreshes it from `BatchEnhancementWorker.file_progress` events.
- [x] Cancel batch run support: stop button terminates the current job and skips remaining queued files, leaving already-completed files intact. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md), [ROADMAP.md](ROADMAP.md). Verification: `BatchCancelToken` is checked before each queued file and inside desktop progress callbacks, raising `BatchCancelledError` to stop the current job cooperatively and skip remaining items.
- [x] Settings persistence: save last-used model, scale, device, CRF, and filter settings to a local JSON config file and restore them on next launch. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: `DesktopSettingsStore` saves/loads model, scale, device, CRF, denoise, and color-correct settings, and desktop UI restores them during startup.
- [x] Recent files list: display a history of previously processed input files so users can re-queue them with a single click without re-browsing. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: `DesktopSettingsStore.remember_files` keeps a unique bounded recent-file list, and desktop UI exposes it through a Recent files dropdown that re-adds selected files to the queue.
- [x] ETA display: show estimated time remaining in the progress area based on current frame throughput, updated each progress tick. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: `DesktopEtaTracker` estimates remaining time from progress ticks and desktop UI updates an ETA label on each progress event.
- [x] Post-job action buttons: show "Open Output Folder" and "Open File" buttons after each job completes so users can immediately review results without leaving the app. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: desktop UI enables Open File and Open Output Folder actions for completed queue entries using `QDesktopServices`.
- [x] Per-file error retry: add a Retry button next to each failed queue entry that re-submits only that file without requiring the user to re-add it manually. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: `DesktopQueueModel.retry_failed` resets failed rows to Pending with progress 0, and desktop UI exposes a Retry Failed control when error rows exist.
- [x] Queue reordering: allow users to drag rows in the file queue table to change processing order before starting the batch. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md). Verification: queue table rows use internal drag/drop and resynchronize into `DesktopQueueModel.reorder_by_input_paths`, with model-level move/reorder tests preserving the requested processing order.
- [x] Output format selector: expose a per-job dropdown (.mp4, .mkv, .mov) in the queue or settings panel that automatically adjusts the output file extension and FFmpeg container. Source: [UI_WORKFLOW.md](../architecture/ui_desktop/UI_WORKFLOW.md), [VIDEO_PIPELINE.md](../architecture/pipeline/VIDEO_PIPELINE.md). Verification: desktop UI exposes an output format dropdown backed by `OUTPUT_FORMATS`, and `DesktopQueueModel.set_all_output_format` updates per-file output paths/configs for `.mp4`, `.mkv`, and `.mov`.

---

## Phase 9: CI/CD & Cross-Platform Release Workflow [Completed]

- [x] GitHub Actions CI workflow: run the full test suite (`python -m unittest`) on every push and pull request for Python 3.9, 3.10, and 3.11 across Ubuntu, macOS, and Windows runners. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: `.github/workflows/ci.yml` defines a push/PR matrix across Ubuntu, macOS, Windows and Python 3.9, 3.10, 3.11, installing the package before running `python -m unittest`.
- [x] Windows release workflow: GitHub Actions job that builds a signed one-file `.exe` installer via PyInstaller and bundles FFmpeg static binaries, ONNX Runtime, and PySide6 for offline distribution. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: `.github/workflows/release-installers.yml` adds a Windows job that installs FFmpeg, discovers PySide6/ONNX Runtime paths, builds one-file CLI and desktop executables with bundled runtime assets, conditionally signs them with `signtool`, and uploads artifacts.
- [x] macOS release workflow: GitHub Actions job that builds a notarized `.dmg` installer via PyInstaller on an `macos-latest` runner, including FFmpeg and CoreML-enabled ONNX Runtime. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: `.github/workflows/release-installers.yml` adds a macOS job that installs FFmpeg, discovers PySide6/ONNX Runtime paths, builds one-file CLI and desktop executables, creates a DMG, conditionally signs with `codesign`, submits notarization with `notarytool`, staples the DMG, and uploads the artifact.
- [x] Linux release workflow: GitHub Actions job that builds a `.deb` package and an AppImage via PyInstaller on an `ubuntu-latest` runner for broad distribution compatibility. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: `.github/workflows/release-installers.yml` adds a Linux job that installs FFmpeg/FUSE tooling, builds one-file CLI and desktop executables with bundled ONNX Runtime/PySide6 assets, packages a Debian artifact with `dpkg-deb`, builds an AppImage with `appimagetool`, and uploads both artifacts.
- [x] Code signing for Windows: integrate Windows code-signing via `signtool` using a self-signed or purchased certificate stored as a GitHub Actions secret. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: Windows tagged releases now fail early when signing secrets are missing, import a base64 `.pfx` from `WINDOWS_SIGNING_CERTIFICATE_BASE64`, sign both one-file executables with SHA-256 timestamping, verify Authenticode signatures, and document required secrets in `.github/signing/README.md`.
- [x] Code signing and notarization for macOS: integrate `codesign` and `notarytool` steps using an Apple Developer certificate stored as a GitHub Actions secret. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: macOS tagged releases now fail early when signing/notarization secrets are missing, import a base64 Developer ID certificate into a temporary keychain, sign with hardened runtime entitlements, verify `codesign`, notarize/staple the DMG with `notarytool`, assess with `spctl`, and document required secrets/entitlements.
- [x] Automated version tagging: on push to `main` with a version bump commit, automatically create a Git tag (`vX.Y.Z`) and trigger the release matrix workflow. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md), [ROADMAP.md](ROADMAP.md). Verification: `.github/workflows/version-tag.yml` watches `pyproject.toml` changes on `main`, reads `[project].version` with `tomllib`, skips existing tags, creates annotated `vX.Y.Z` tags, and pushes them to trigger the release workflow.
- [x] GitHub Releases artifact upload: after each platform build succeeds, upload the `.exe`, `.dmg`, and `.deb`/AppImage to a GitHub Release draft with auto-generated release notes from `CHANGELOG.md`. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: `release-installers.yml` now grants `contents: write`, waits for all platform installer jobs, downloads uploaded artifacts, extracts matching release notes from `CHANGELOG.md`, and creates a draft GitHub Release with `gh release create`.
- [x] Build matrix smoke test: after each installer is built, run a headless CLI `--help` and `--dry-run` smoke check inside the same CI job to confirm the packaged binary is not broken before upload. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md), [TROUBLESHOOTING.md](../architecture/operations/TROUBLESHOOTING.md). Verification: Windows, macOS, and Linux release jobs run the packaged CLI executable with `--help` and `enhance ... --dry-run` before signing/package upload.
- [x] Dependency caching in CI: cache `.venv`, pip wheels, and PyInstaller build artifacts between workflow runs to reduce average build time. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: CI and release workflows use `actions/setup-python` pip caching plus `actions/cache@v4` entries for `.venv`, pip cache directories, and PyInstaller `build` output keyed by OS, Python version, and dependency manifests.

---

## Phase 10: GitHub Repository Presence [Completed]

- [x] GitHub repository About section: set a concise description, website URL, and topic tags (e.g., `video-enhancement`, `upscaling`, `onnx`, `ffmpeg`, `pyside6`, `ai`, `python`) so the repo is discoverable. Source: [README.md](../../README.md). Verification: `.github/repository.yml` records the repository description, homepage, enabled Discussions setting, default branch, and topic list, and README documents the values for GitHub repository settings.
- [x] README social preview image: create and attach a 1280×640 Open Graph image to the repository so link previews on Twitter, LinkedIn, and Discord show a branded visual. Source: [README.md](../../README.md). Verification: `docs/assets/github-social-preview.svg` provides a 1280×640 branded preview asset and README links it for repository social preview upload.
- [x] Pinned release badges in README: add dynamic shields.io badges for latest release version, total downloads, and platforms supported, linked to the GitHub Releases page. Source: [README.md](../../README.md). Verification: README includes dynamic GitHub release, total downloads, platform, package, status, and license badges linked to release/package destinations.
- [x] GitHub Releases page: publish the first tagged release (`v0.1.0`) with a formatted release body derived from `CHANGELOG.md`, listing new features, known limitations, and installation instructions. Source: [CHANGELOG.md](../../CHANGELOG.md), [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: Phase 9 release workflow creates draft GitHub Releases for tagged builds, extracts matching release notes from `CHANGELOG.md`, and uploads versioned installer artifacts.
- [x] Release tag convention and policy: document the `vMAJOR.MINOR.PATCH` scheme in `CONTRIBUTING.md` and define what constitutes a patch, minor, or major release for this project. Source: [CONTRIBUTING.md](../../CONTRIBUTING.md). Verification: CONTRIBUTING documents SemVer tag format, patch/minor/major criteria, changelog/version bump process, and automatic tag creation from `pyproject.toml`.
- [x] GitHub Packages (PyPI-compatible): publish `silukman-video-enhancer` as a Python package artifact so it can be downloaded from the repository release flow. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: `.github/workflows/publish-package.yml` builds source/wheel distributions, uploads them as workflow artifacts, creates a draft release when needed, and attaches the package files to the tagged GitHub Release.
- [x] Release asset naming convention: standardize installer filenames to `silukman-video-enhancer-vX.Y.Z-windows-x64.exe`, `...-macos-arm64.dmg`, `...-linux-x86_64.deb`, and `...-linux-x86_64.AppImage` for consistent artifact identification. Source: [BUILD_AND_PACKAGING.md](../architecture/deployment/BUILD_AND_PACKAGING.md). Verification: release workflow copies/builds versioned Windows, macOS, Debian, and AppImage artifacts, and `BUILD_AND_PACKAGING.md` documents the naming convention.
- [x] GitHub Discussions or issue templates: add issue templates for bug reports, feature requests, and model requests so contributor submissions include enough context to be actionable. Source: [CONTRIBUTING.md](../../CONTRIBUTING.md). Verification: `.github/ISSUE_TEMPLATE/` includes bug report, feature request, model request, and config templates, while repository metadata enables Discussions.
- [x] Security policy (`SECURITY.md`): publish a responsible disclosure policy and contact channel so security researchers know how to report vulnerabilities privately. Source: [ROADMAP.md](ROADMAP.md). Verification: `SECURITY.md` defines supported versions, private reporting through GitHub Security Advisories, vulnerability scope, and excluded reports.
